{
	"name": "sam-workspace",
	"workspaces": [
		"libraries/*",
		"packages/*"
	],
	"devDependencies": {
		"@types/node": "^18.14.6",
		"esbuild": "^0.17.11",
		"typescript": "^4.9.5"
	},
	"type": "module",
	"scripts": {
		"build": "node build.js",
		"del": "rm -rf .aws-sam/ && rm -rf node_modules/ && rm -rf package-lock.json",
		"start:local": "sam local start-api --parameter-overrides \"Stage=local UserTableName=users \""
	}
}
